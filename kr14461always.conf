set -as terminal-features ',*:RGB'
set -s extended-keys always
set -as terminal-features ',*:extkeys'
bind -T root C-\; run-shell 'echo PARSED-AS-C-SEMICOLON >> /tmp/kr14461always.saw'
bind -T root ';'  run-shell 'echo PARSED-AS-PLAIN-SEMICOLON >> /tmp/kr14461always.saw'
