set -as terminal-features ',*:RGB'
set -s extended-keys on
set -as terminal-features ',*:extkeys'
bind -T root C-\; run-shell 'echo PARSED-AS-C-SEMICOLON >> /tmp/kr8730on.saw'
bind -T root ';'  run-shell 'echo PARSED-AS-PLAIN-SEMICOLON >> /tmp/kr8730on.saw'
